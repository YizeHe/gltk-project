# Reverse-engineering fixtures for Chapter 7

Regenerate:

```powershell
cd D:\grokbuild\groklang\gltk
go run learn\testdata\re\gen_fixtures.go
```

These files are **synthetic / safe** for teaching. For real malware samples use your own lab corpus under `testfile/` with proper authorization.
