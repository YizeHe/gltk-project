// hello.glk — minimal GLVM sample
import out

fn main(args) {
  out.println("hello from GrokLang GLVM")
  let x = 1
  let s = "hi"
  if x > 0 {
    out.println(s)
  }
  while x < 3 {
    x = x + 1
  }
  for i in range(0, 3) {
    out.println(i)
  }
  return 0
}
