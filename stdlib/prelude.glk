// Optional prelude helpers (not auto-imported).
// Import with: import "prelude.glk" as prelude  (via stdlib / GLTK_LIB)

fn min(a, b) {
  if a < b { return a }
  return b
}

fn max(a, b) {
  if a > b { return a }
  return b
}
